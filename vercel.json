{
  "builds": [
    {
      "src": "src/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "src/dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/dist/index.html"
    }
  ]
}
